---
layout: post
title: tomcatでバックグラウンドプロセスを正しく作る方法
date: 2015-07-25 05:59:57
categories: java tomcat
---
<!-- {% raw %} -->
<p>tomcatプロジェクトで、バックグラウンドで常に動いている処理を作りたいです。<br>
例えば、数分ごとに定期実行する処理があって、それをtomcatのサーブレットでも使いたい という感じです。</p>

<p>もちろん、数分ごとの処理はcronで実行してtomcatとは別のプロセスでやるのがベストだとは思うのですが、一つのtomcatプロジェクトで全て完結して、サーブレットから定期実行のスレッドのインスタンスにアクセスしたいのでtomcat上で動かす手段を探しています。</p>

<p>単純に、web.xmlのListenerタグを使い、tomcatの起動時にスレッドを作り、ServletContextのattributeにセットする という方法でやれば出来るのですが<br>
tomcatに組み込まれている公式の方法 というのがあればそちらを使いたいです。</p>
<!-- {% endraw %} -->
